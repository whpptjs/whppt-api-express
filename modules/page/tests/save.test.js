const test = require('ava');
const sinon = require('sinon');

test('can be created', t => {
  // Arrange
  // Act
  // Assert
});

test('can be updated', t => {});
test('requires a page to save', t => {});
